{
  "name": "thumb-pipeline-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": ".\\.venv\\Scripts\\python -m unittest discover -s backend/tests -v",
    "test:frontend": "node --test frontend/tests/*.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "concurrently": "^9.2.1"
  }
}
